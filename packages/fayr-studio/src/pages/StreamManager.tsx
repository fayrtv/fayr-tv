import React, { FunctionComponent } from "react";

interface OwnProps {}

type Props = OwnProps;

const StreamManager: FunctionComponent<Props> = (props) => {
    return <></>;
};

export default StreamManager;
