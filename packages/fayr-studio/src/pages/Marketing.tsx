import React, { FunctionComponent } from "react";

interface OwnProps {}

type Props = OwnProps;

const Marketing: FunctionComponent<Props> = (props) => {
    return <></>;
};

export default Marketing;
