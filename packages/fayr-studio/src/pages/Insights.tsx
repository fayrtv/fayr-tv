import React, { FunctionComponent } from "react";

interface OwnProps {}

type Props = OwnProps;

const Insights: FunctionComponent<Props> = (props) => {
    return <></>;
};

export default Insights;
